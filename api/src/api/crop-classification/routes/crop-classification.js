'use strict';

/**
 * crop-classification router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::crop-classification.crop-classification');
