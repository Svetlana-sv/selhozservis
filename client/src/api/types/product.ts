export type Product = {
  id: number
  attributes: {
    title: string
    image: string
    description: string
  }
  // category_id: number // category: number || string ??
  // price: number
  // count: number
  // packing_id: number // packing: number || string ??
}